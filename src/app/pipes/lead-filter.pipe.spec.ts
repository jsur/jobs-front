import { LeadFilterPipe } from './lead-filter.pipe';

describe('LeadFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LeadFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
