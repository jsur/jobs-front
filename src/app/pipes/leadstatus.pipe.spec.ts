import { LeadstatusPipe } from './leadstatus.pipe';

describe('LeadstatusPipe', () => {
  it('create an instance', () => {
    const pipe = new LeadstatusPipe();
    expect(pipe).toBeTruthy();
  });
});
